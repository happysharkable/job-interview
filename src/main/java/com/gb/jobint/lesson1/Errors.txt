Найденные ошибки в задании 2 из методички:

1. В классе Car поля должны быть указаны как protected для его наследников;
2. Класс Lorry наследуется (extends) от интерфейсов вместо их имплементации (implements);
3. В классе Lorry отсутствуют аннотации @Override над переопределяемыми реализуемыми методами.

Предложения:

1. При увеличении количества свойств объекта Car со временем можно перейти на паттерн "строитель" 
для облегчения настройки объекта;